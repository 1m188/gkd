#

## 整体功能目标
隐藏文件或者信息到图片中

## 具体功能拆解
- 该程序本质为一个CLI程序，支持命令行调用，通过命令行传入目标图片路径和待隐藏文件路径或者待隐藏文本信息，来实现将待隐藏文件或者文本信息隐藏到目标图片中，并生成新的图片文件
- 原理：利用数字隐写术，将待隐藏信息写入到如PNG图片的注释块，或是JPEG的APPn应用数据段等格式协议预留的冗余字段中
- 解码：该程序在实现了隐藏功能的基础上还应该实现解码功能，即从图片中提取出隐藏的信息，这里基于上述原理，在不同格式的图片的基础上进行对应的提取操作

## 技术选型
- 使用golang语言实现，具体标准版本参见[go.mod](./go.mod)

## 架构设计
- 目前已有main.go文件，在其中应该实现命令行调用逻辑，包括参数解析、调用隐藏/解码函数、输出结果等
- 隐藏/解码函数应该单独封装到不同的文件中，如hide.go和decode.go，分别实现隐藏和提取功能
- 所有文件都在根目录中，也即在同一个包下面

## 约束
- 尽可能使用golang语言自身完善的标准库，除非标准库实在是无法实现的功能，才考虑使用第三方库
- 尽可能使用golang语言自身提供的工具，如go mod、go build、go test等，避免使用第三方工具
- 尽可能使用golang语言自身提供的测试框架，如testing包，避免使用第三方测试框架
- 尽可能保证函数功能的简单清晰和直接，避免过度设计和冗余内容
- 添加必要和清晰的注释，特别是对于复杂的逻辑和算法
- 编写代码完成之后要进行测试，使用golang语言自带的testing包进行单元测试，确保代码的正确性。除此之外还需要实际编译出产物之后在命令行里面进行实际的测试，确保功能正确。
